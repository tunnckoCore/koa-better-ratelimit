{
  "name": "koa-better-ratelimit",
  "version": "1.0.1",
  "description": "Better, smaller, faster - koa middleware for limit request by ip, store in-memory.",
  "main": "index.js",
  "scripts": {
    "lint": "npm install && ./node_modules/.bin/jshint ./*.js",
    "test": "npm run lint && NODE_ENV=test ./node_modules/.bin/mocha --require should --harmony-generators",
    "test-cov": "npm test && NODE_ENV=test node --harmony-generators node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha -- -u exports --require should",
    "test-travis": "npm test && NODE_ENV=test node --harmony-generators node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha --report lcovonly -- -u exports --require should"
  },
  "author": {
    "name": "Charlike Make Reagent",
    "email": "mameto_100@mail.bg",
    "url": "https://github.com/tunnckoCore",
    "twitter": "https://twitter.com/tunnckoCore"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/tunnckoCore/koa-better-ratelimit.git"
  },
  "keywords": [
    "koa",
    "middleware",
    "ratelimit",
    "requestlimit",
    "limit",
    "in-memory",
    "memorystore",
    "rate"
  ],
  "dependencies": {
    "ipchecker": "*"
  },
  "devDependencies": {
    "coveralls": "*",
    "istanbul-harmony": "~0.3.0",
    "jshint": "*",
    "koa": "*",
    "mocha": "*",
    "mocha-lcov-reporter": "*",
    "should": "*",
    "supertest": "*"
  },
  "licenses": [{
    "type": "MIT",
    "url": "https://github.com/tunnckoCore/koa-better-ratelimit/blob/master/license.md"
  }]
}
